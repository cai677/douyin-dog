---
name: jianying-voice-draft
description: 根据飞书拆解文档生成剪映朗读字幕草稿，或根据用户直接提供的文案和音色按标点逐句配音；支持首尾气口处理、1.2/1.3/1.4倍速试听、用户选定倍速后完成逐句音频及可编辑剪映草稿。
---

# 剪映朗读音频字幕轨

支持飞书拆解文档生成剪映草稿，以及直接文案生成逐句朗读音频两种场景。草稿只包含朗读音频轨和字幕轨，不处理画面素材。

## 适用场景

使用本 skill 当用户要求：

- 根据飞书拆解文档生成剪映草稿；
- 直接提供文案和音色，按标点分句，试听1.2、1.3、1.4倍速后完成朗读；
- 只生成朗读音频轨和字幕轨；
- 使用豆包/火山语音 TTS；
- 一句文案对应一个独立朗读音频片段；
- 所有句子的朗读倍速一致；
- 去掉每句开头多余气口。

如果用户要求拆解视频、抽帧、生成带图片分镜文档，应使用带货视频拆解 skill，而不是本 skill。

## 最终交付要求（用户确认）

默认最终目标是可编辑剪映草稿。试听、选倍速、逐句配音都是中间步骤；用户选定倍速后继续完成全篇、对齐无标点字幕、生成并安装草稿，不再询问是否要草稿，不以音频或压缩包作为任务完成。仅用户明确要求只要音频时例外。字体默认系统字体、字号9，当前用户指定优先。完成报告给出草稿名、路径、片段数量及真实验证状态。

## 直接文案配音场景（2026-09-15）

用户只提供整篇文案并选择音色时，读取并执行 [直接文案与倍速试听流程](references/text-audition.md)。不要求飞书文档或参考时间轴。

按标点逐句断行，去掉断行文案中的标点，按下文直接执行规则核对音色名称、完整ID和配音要求；用户要求试听时，试听阶段仅生成前三个非空断句（不足三句则全部），每句裁掉首尾多余气口，再从同一裁后原音频制作1.2x、1.3x、1.4x保音高试听。前三句各提供三个倍速版本，等用户选定统一倍速后才生成剩余句子，完成全篇独立逐句朗读及剪映草稿。不得提前生成全篇试听。连续试听仅作补充。

本场景以该流程为准，不套用下文旧版飞书专用的连续重复去重、只裁句首、强制原总时长要求。旧版voice_draft.py尚无直接文案和三档试听入口；规范已定义不代表脚本已实现或音频已生成。

## 新任务直接执行（用户授权，2026-09-23）

用户希望保存豆包凭据并在提出明确需求后直接获得结果。本节取代旧版每次配音前一律要求再次确认的规则。

- 已知音色按下表名称映射完整ID；用户已提供明确文案、音色和单一倍速时，简短告知采用设置后直接生成全篇及安装剪映草稿，不重复索要确认，不强制插入试听。用户明确只要音频时按要求交付音频。
- 用户要求多档试听时，仍只生成前三个非空断句，处理首尾气口后提供指定倍速，等待选择；选定后直接完成全篇及草稿。不得擅自挑选倍速。
- 未指定语气时采用音色默认，气口复用对应已确认标准或现行紧凑规则，默认系统字体、字号9；不改写文案，按真实音频时长排列。新音色未校准状态如实记录。
- 仅缺少必要信息、音色名称/ID有歧义、服务不可用或用户要求先确认时集中询问。新名称与ID需建立明确对应关系，不能自动替换不可用音色。
- 本次音色、倍速与例外保存在任务目录，不改变其他任务默认值。

## 本机豆包凭据复用

2026-09-23 用户授权将豆包 API Key 保存在当前 Windows 用户的凭据管理器，目标名为 `Codex/jianying-voice-draft/DoubaoTTS`。新任务先使用 `scripts/doubao_credential.py` 读取，不再让用户反复输入。凭据与聊天无关，仅本机当前 Windows 用户可复用；不承诺其他电脑自动同步。

- 只检查可用状态：`python <skill>/scripts/doubao_credential.py status`，不输出密钥。
- 执行任务：`python <skill>/scripts/doubao_credential.py run <任务目录>/generate.py`；包装器在当前进程中注入 `DOUBAO_TTS_API_KEY`，执行结束后恢复环境。任务脚本必须禁止打印环境变量、请求认证头或密钥。
- 也可在 Python 内从该模块导入 `read_key()`，只传给豆包官方接口。不得通过命令行参数、日志、JSON、文档、Git或聊天再次输出密钥；不要运行会打印密钥的调试命令。
- Windows沙箱可能使用隔离账户，无法读取主机凭据。此时（例如1168）先使用受批准的主机执行环境（exec_command的require_escalated）运行包装器，不要当成密钥丢失或让用户重新输入。只有主机当前用户也确实缺少凭据，或官方接口证实密钥失效时，才请求用户通过本机隐藏输入框更新。`store-env` 可从当前进程环境写入并校验，不接受明文命令行密钥参数。
- 当前已成功使用的接口是 `https://openspeech.bytedance.com/api/v3/tts/unidirectional/sse`，内置音色资源 `seed-tts-2.0`，已验证小天自定义音色资源 `seed-icl-2.0`。其他自定义音色仍按真实服务结果检查。不要因旧示例的资源ID不同就反复要求重新开通。
- 此包装器只解决凭据复用；配音、首尾裁剪、变速、草稿生成和安装仍按本skill流程完成，不把保存密钥视为生产完成。

### 已知音色名称与ID（用户指定，2026-09-15）

| 音色名称 | 完整音色ID | 气口标准及记录 |
| --- | --- | --- |
| 佩奇 | `zh_female_peiqi_uranus_bigtts` | 待独立校准 |
| 樱桃丸子 | `zh_female_yingtaowanzi_uranus_bigtts` | 待独立校准 |
| 直率英子 | `zh_female_zhishuaiyingzi_uranus_bigtts` | 用户标记03；参见下方已确认标准及试听参考 |
| 暖阳女声 | `zh_female_kefunvsheng_uranus_bigtts` | 用户标记02；待独立校准 |
| 油腻大叔 | `ICL_uranus_zh_male_younidashu_tob` | 用户标记04；用户认可当前粮勺04句尾效果，保留，不因其他音色调整而联动修改 |
| 熊二 | `zh_male_xionger_uranus_bigtts` | 待独立校准 |
| 知性女声 | `zh_female_zhixingnv_uranus_bigtts` | 待独立校准 |
| 猴哥 | `zh_male_sunwukong_uranus_bigtts` | 待独立校准 |
| 小天 | `S_LbDgjHwe2` | 用户提供的自定义音色；服务可用性及气口待实测 |
| 庆梓 | `S_nJsfjHwe2` | 用户提供的自定义音色；服务可用性及气口待实测 |

以上名称与ID来自用户提供的清单，收录不代表服务可用性或试听已验证。长期以名称和完整ID识别音色；编号仅作为用户标记，不据此自动分配未来任务。新增音色按下文档案记录名称与ID；信息明确时可直接执行，气口独立校准后保存可复用标准。

历史任务说明：本次粮勺02最初指定暖阳女声，因当时服务返回资源未授权，经用户确认改用直率英子生成。此音色清单不改变已生成的02配音或用户手工编辑稿；以后选择暖阳女声时按当次授权重新检查可用性，不能自动替换。

## 已确认音色气口标准

- 使用 `zh_female_zhishuaiyingzi_uranus_bigtts` 时先读取 [音色标准](references/voice-profile-zhishuaiyingzi.json)，优先复用用户已确认的20ms句首、60ms句尾及检测设置。试听参考保存在用户本机；仓库仅保存参数，不分发试听音频。
- 2026-09-15用户确认的是前六句连续试听、原始语速1.0x。气口参数与语速分别记录；不得声称此前草稿的1.098x也经过这次试听确认。
- 使用现有 `compact_audio.py` 时显式传入 `--head-ms 20 --tail-ms 60 --threshold-db -42`。当前脚本不会自动加载音色配置，执行者必须按音色读取并应用。
- 同音色新句子仍检查起尾字与连续衔接，参数复用不等于新音频自动通过试听。不同音色独立校准，不套用本音色已批准状态。

## 紧凑气口规则（2026-09-15）

用户要求收紧头尾气口、减少留白时，采用本节；优先于下文旧版“60ms句首、不裁句尾、强制原总时长”约束。

- 句首保留20ms，句尾保留60ms。低声起音或拖尾有误切风险时局部增加缓冲并记录，不能只为变短切掉发音。
- 使用10ms窗RMS、起步门限-42dBFS、连续三窗中至少两窗有效；不能把单个越过阈值的杂音点当作起声。低电平不等于无语音，门限是启发式而非语音识别。
- 用 `scripts/compact_audio.py <输入.wav> <新输出.wav>` 裁两端并保存采样区间记录。它只处理PCM16单声道WAV，保留句中音频；不自动裁掉句中轻声或呼吸。
- 修订已有草稿时保持修订前统一倍速，按裁后真实长度连续排列音频和字幕，允许总时长缩短。禁止通过降速、补空白或拉长字幕把移除的停顿填回去。必须保持原时长的任务另行确定节奏方案。
- 默认每句独立WAV，不合并交付；只改用户指定草稿，原稿另存备份，不覆盖用户手工编辑稿。字体遵循当前用户指定。
- 保存每句前后留白、移除秒数、源采样区间和时间轴；检查首字、尾字、长停顿及字幕对齐。数值检测通过不能替代试听，未试听明确标记。
- `voice_draft.py` 是旧版入口，尚未集成本节的双端裁剪和时间线规则，不得直接运行并声称完成紧凑模式；应调用新裁剪器并按本节重建时间线。

## 旧版飞书脚本约束（仅历史入口）

本节描述旧脚本行为，不覆盖上文已确认的系统字体、首尾气口和直接文案交付规则。

- 飞书文档必须包含可解析文案表格。优先读取第六段“逐句文案时间轴”里的 `时间` 和 `文案` 列；如果第六段缺失或不可解析，再回退读取表头里的 `时间` 和 `台词`/`口播文案`/`字幕`/`文案` 列。
- 若文档来自 `daihuo-video-breakdown`，优先读取第六段“逐句文案时间轴”表格；该表格列顺序固定为：序号 / 时间 / 文案。第五段“完整分镜脚本”只作为回退来源，列顺序固定为：镜号 / 时间 / 时长 / 景别 / 台词 / 画面帧 / 音效。
- 如果文档没有单独 `视频时长 | XX.XX 秒` 字段，用实际读取到的文案表格最后一行 `时间` 的结束点作为总时长；如果解析不到时间列或文案/台词列，先回到拆解文档补齐交接字段。
- 连续重复文案只朗读一次；非连续重复保留。
- 如果文档有单独视频总时长且大于表格最后一行结束时间，则最后一句延长到视频总时长。
- 每句文案单独生成 WAV，不合并为一条总音频。
- 每个 WAV 重写标准 WAV 头，避免剪映解析异常。
- 每句只裁剪句首静音，保留约 60ms 缓冲；不裁剪句尾。
- 所有音频片段使用同一个统一倍速。
- 字幕按飞书表格时间轴排列。
- 字幕字体名写入“抖音体”，字号写入 9。

## 本机依赖

优先使用当前项目或本机已验证的 Python：

```powershell
C:\Users\Administrator\.workbuddy\binaries\python\envs\default\Scripts\python.exe
```

脚本需要：

- `pyJianYingDraft`
- `pymediainfo`
- 飞书 CLI：`C:\Users\Administrator\.workbuddy\binaries\node\cli-connector-packages\node_modules\@larksuite\cli\bin\lark-cli.exe`

豆包 API Key 由上文凭据包装器提供，不写入明文文件或命令行参数。下列旧版脚本示例仅适用于其飞书流程，不能代替直接文案流程。

## 运行脚本

脚本在本 skill 的 `scripts/voice_draft.py`。

```powershell
# 通过 scripts/doubao_credential.py run 包装下面的任务入口，自动从本机读取凭据

& 'C:\Users\Administrator\.workbuddy\binaries\python\envs\default\Scripts\python.exe' `
  '<skill-folder>\scripts\voice_draft.py' `
  --doc-url '<飞书文档链接>' `
  --voice-id 'zh_male_xionger_mars_bigtts' `
  --draft-name '<剪映草稿名>'
```

当前已验证语音参数：

```text
resource_id: volc.service_type.10029
voice_id: zh_male_xionger_mars_bigtts
```

## 输出

剪映草稿写入：

```text
C:\Users\Administrator\AppData\Local\JianyingPro\User Data\Projects\com.lveditor.draft\<draft-name>
```

中间音频写入 skill 目录下：

```text
outputs\*\lines
outputs\*\lines_trimmed
```

## 旧版飞书脚本验收

以下仅适用于旧版飞书时间轴入口；直接文案按 references/text-audition.md 验收，不要求原总时长或抖音体。

生成后检查：

1. 剪映草稿能打开。
2. 音频轨是多段独立 WAV，不是合并 MP3 或合并 WAV。
3. 音频片段数等于字幕片段数。
4. 所有音频片段的 speed 完全一致。
5. 草稿总时长等于飞书文档视频总时长；若文档未单独标注视频时长，则等于实际读取到的文案表格最后一行结束时间。
6. 连续重复文案没有重复朗读。
7. 每句起声没有明显句首空白。
8. 字幕显示为抖音体、9 号；若本机缺字体，剪映可能回退，需要人工确认。

## 参考文档

完整工作流、踩坑记录和复用说明见 [references/workflow.md](references/workflow.md)。当用户要求复用、维护或解释该剪辑工作流时，先读取该参考文档。

## 2026-09-13 固定上游格式补充

`daihuo-video-breakdown` 后续固定输出当前六段式飞书文档格式时，`jianying-voice-draft` 优先读取第六段“逐句文案时间轴”表格。表格列顺序固定为：序号 / 时间 / 文案。

如果第六段缺失或不可解析，再回退读取第五段“完整分镜脚本（含画面帧）”表格。表格列顺序固定为：镜号 / 时间 / 时长 / 景别 / 台词 / 画面帧 / 音效。

当文档没有单独的 `视频时长 | XX.XX 秒` 字段时，不再判定为错误；用实际读取到的文案表格最后一行 `时间` 的结束点作为剪映草稿总时长。

